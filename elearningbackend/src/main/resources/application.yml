server:
  port: 8080
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: root
        url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/elearningInc
        username: root
    jpa:
        show-sql: true  
        hibernate:
            ddl-auto: update